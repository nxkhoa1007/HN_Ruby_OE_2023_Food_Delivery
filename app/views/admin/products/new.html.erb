<%= provide :title, t("text.add_product") %>
<%= provide :button_text, t("text.add") %>
<div class="card mb-4">
  <div class="card-header fw-bold"><%= t"text.add_product" %></div>
  <div class="card-body">
    <div class="tab-content rounded-bottom">
      <%= render "form", { :url => admin_products_path} %>
    </div>
  </div>
</div>
